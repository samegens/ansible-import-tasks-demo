# ansible-import-tasks-demo

Shows that dynamic variables can be used in [import_tasks](https://docs.ansible.com/ansible/latest/modules/import_tasks_module.html).

Run with

    ansible-playbook -i inventory playbook.yml